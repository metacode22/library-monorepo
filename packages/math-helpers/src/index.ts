/**
 *
 * @param a
 * @param b
 * @returns a + b
 */
export function add(a: number, b: number) {
  return a + b;
}

/**
 *
 * @param a
 * @param b
 * @returns a - b
 */
export function subtract(a: number, b: number) {
  return a - b;
}
